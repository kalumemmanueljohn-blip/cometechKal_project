{
  "name": "cometech-project",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "dev:backend": "cd backend-typescript && npm run start:dev",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
